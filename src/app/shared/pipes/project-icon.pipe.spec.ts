import { ProjectIconPipe } from './project-icon.pipe';

describe('ProjectIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectIconPipe();
    expect(pipe).toBeTruthy();
  });
});
